1. 메모리 안에 존재하는 사용자 프로그램 A가 CPU에 I/O를 요청하기 위해서는 ( ) 인터럽트를 실행해야 한다.
  
2. 보안을 해칠 수 있는 중요한 명령어는 모니터 모드에서만 수행가능한 특권명령으로 규정한다 이 때 모니터 모드는 mode bit를 ( )으로 설정한 상태이다.
  
3. 입출력 방식의 하나로 I/O 요청 이후 구현하기 위해 I/O가 끝날 떄까지 CPU를 낭비시키거나 I/O가 완료될 때까지 해당 프로그램에게서 CPU를 빼앗는 방식은 무엇인가?
  
4. I/O 디바이스의 작업은 CPU에서 직접 처리하지 않고 각 디바이스의 디바이스 ( )이 처리한다. 디바이스 ( )은 장치제어기라고 불리며 각 장치들을 통제하는 일종의 작은 CPU의 역할을 한다